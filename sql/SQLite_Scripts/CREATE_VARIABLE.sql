CREATE TABLE VARIABLE(ID INT PRIMARY KEY      NOT NULL,
                      NAME           CHAR(50) NOT NULL,
                      VALUE          TEXT     NOT NULL,
                      DESCRIPTION    TEXT )